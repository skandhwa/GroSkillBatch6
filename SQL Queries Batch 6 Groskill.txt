CREATE TABLE mypracticeschema.employees11 (
    id   INT NOT NULL PRIMARY KEY  UNIQUE,
    name VARCHAR(100)  ,
    address VARCHAR(255) ,
    age INT CHECK(age>=18),
    salary DECIMAL(10,2),
    status varchar(10) DEFAULT 'ACTIVE'
);

select * from mypracticeschema.employees11;

SELECT salary
FROM (
    SELECT salary,
           DENSE_RANK() OVER (ORDER BY salary DESC) AS rnk
    FROM mypracticeschema.employees11
) t
WHERE rnk = 4;









INSERT INTO mypracticeschema.employees11 (id, name, address, age, salary)
VALUES (1, 'Rahul Sharma', 'Bangalore, India', 22, 55000.00);

INSERT INTO mypracticeschema.employees11 (id, name, address, age, salary, status)
VALUES (2, 'Anita Verma', 'Mumbai, India', 32, 72000.50, 'ACTIVE');

INSERT INTO mypracticeschema.employees11 (id, name, address, age, salary, status)
VALUES (3, 'Suresh Kumar', 'Chennai, India', 45, 98000.75, 'INACTIVE');

INSERT INTO mypracticeschema.employees11 (id, name, address, age, salary)
VALUES (4, 'Neha Patel', 'Ahmedabad, India', 24, 42000.00);

INSERT INTO mypracticeschema.employees11 (id, name, address, age, salary, status)
VALUES (5, 'Amit Singh', 'Delhi, India', 38, 85000.00, 'ACTIVE');

INSERT INTO mypracticeschema.employees11 (id, name, address, age, salary, status)
VALUES (78, 'Amit Singh', 'Delhi, India', 43, 85000.00, 'ACTIVE');

select * from mypracticeschema.employees11;

INSERT INTO mypracticeschema.employees11 (id, name, address, age, salary, status)
VALUES (6, 'Amit Singh', 'Delhi, India', 28, 85000.00,'');

delete from mypracticeschema.employees11 where id=6;


ALTER TABLE mypracticeschema.employees11
ADD department VARCHAR(50);

UPDATE mypracticeschema.employees11 SET `department` = 'Finance' WHERE (`id` = '1');
UPDATE mypracticeschema.employees11 SET `department` = 'Marketing' WHERE (`id` = '2');
UPDATE mypracticeschema.employees11 SET `department` = 'IT' WHERE (`id` = '3');
UPDATE mypracticeschema.employees11 SET `department` = 'Finance' WHERE (`id` = '4');
UPDATE mypracticeschema.employees11 SET `department` = 'Operations' WHERE (`id` = '5');

ALTER TABLE mypracticeschema.employees11
MODIFY department VARCHAR(200) NOT NULL;


SELECT * FROM mypracticeschema.employees11
WHERE salary > 70000 OR age>40;

SELECT * FROM mypracticeschema.employees11
WHERE  NOT department = 'Finance';

SELECT *
FROM mypracticeschema.employees11
WHERE department NOT IN ('Finance', 'Operations');

select * from  mypracticeschema.employees11 where name like '%Ra__l%';

select SUM(salary) as MAX_SALARY from mypracticeschema.employees11;

select count(id) AS total_count from mypracticeschema.employees11;

SELECT *
FROM mypracticeschema.employees11
ORDER BY salary;

SELECT *
FROM mypracticeschema.employees11
ORDER BY salary DESC;
 
select department,count(*) AS Total_Count from mypracticeschema.employees11 group by department ;

SELECT department, AVG(salary) AS avg_salary
FROM mypracticeschema.employees11
GROUP BY department
HAVING AVG(salary) > 70000;

SELECT department, count(*) as TOTAL_COUNT
FROM mypracticeschema.employees11
GROUP BY department
HAVING Count(*) > 1;


CREATE TABLE mypracticeschema.departmentDelete
(
    dept_id INT PRIMARY KEY,
    dept_name VARCHAR(100),
    location VARCHAR(100)
);

INSERT INTO mypracticeschema.departmentDelete (dept_id, dept_name, location)
VALUES
(1, 'IT', 'Bangalore'),
(2, 'HR', 'Mumbai'),
(3, 'Finance', 'Delhi');

select * from mypracticeschema.departmentDelete;

delete from mypracticeschema.departmentDelete where dept_id=1;

truncate table mypracticeschema.departmentDelete;

drop table mypracticeschema.departmentDelete;

Alter table mypracticeschema.departmentDelete drop column location;


CREATE TABLE mypracticeschema.employees12 (
    emp_id INT,
    emp_name VARCHAR(50),
    department VARCHAR(50),
    salary INT
);


INSERT INTO mypracticeschema.employees12 (emp_id, emp_name, department, salary)
VALUES
(1, 'Rahul', 'IT', 70000),
(2, 'Amit', 'HR', 60000),
(3, 'Priya', 'IT', 70000),
(4, 'Sneha', 'Finance', 80000),
(5, 'Ravi', 'HR', 60000),
(6, 'Anita', 'IT', 90000),
(6, 'Anita', 'IT', 90000),
(7, 'Karan', 'Finance', 80000);

select * from mypracticeschema.employees12;

SELECT DISTINCT *
FROM mypracticeschema.employees12;

select Count(distinct department) AS TotalUniqueDept from mypracticeschema.employees12;

CREATE TABLE mypracticeschema.employees13 (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    department_id INT,
    manager_id INT,
    salary DECIMAL(10,2),
    city VARCHAR(50)
);


CREATE TABLE mypracticeschema.departments13 (
    department_id INT PRIMARY KEY,
    department_name VARCHAR(50),
    location VARCHAR(50)
);

INSERT INTO mypracticeschema.employees13 
(emp_id, emp_name, department_id, manager_id, salary, city)
VALUES
(101, 'Rahul', 10, NULL, 120000, 'Hyderabad'),
(102, 'Amit', 10, 101, 80000, 'Hyderabad'),
(103, 'Priya', 10, 101, 75000, 'Bangalore'),
(104, 'Sneha', 20, 105, 70000, 'Bangalore'),
(105, 'Vijay', 20, NULL, 110000, 'Bangalore'),
(106, 'Arjun', 30, 105, 65000, 'Mumbai'),
(107, 'Neha', 40, NULL, 90000, 'Delhi'),
(108, 'Kiran', 60, 107, 60000, 'Chennai');

INSERT INTO mypracticeschema.departments13 (department_id, department_name, location)
VALUES
(10, 'IT', 'Hyderabad'),
(20, 'HR', 'Bangalore'),
(30, 'Finance', 'Mumbai'),
(40, 'Sales', 'Delhi'),
(50, 'Marketing', 'Pune');

select * from  mypracticeschema.employees13 ;

select * from  mypracticeschema.departments13;

SELECT 
    e.emp_id,
    e.emp_name,
    e.salary,
    d.department_name,
    d.department_id
FROM mypracticeschema.employees13 e
INNER JOIN mypracticeschema.departments13 d
    ON e.department_id = d.department_id;
    
    
    SELECT 
    e.emp_id,
    e.emp_name,
    d.department_name
FROM mypracticeschema.employees13 e
LEFT JOIN mypracticeschema.departments13 d
    ON e.department_id = d.department_id;
    
    
    SELECT 
    e.emp_id,
    e.emp_name,
    d.department_name
FROM employees e
LEFT JOIN departments d
    ON e.department_id = d.department_id

UNION

SELECT 
    e.emp_id,
    e.emp_name,
    d.department_name
FROM employees e
RIGHT JOIN departments d
    ON e.department_id = d.department_id;
    
    
    SELECT 
    e.emp_id,
    e.emp_name,
    d.department_name,
    d.department_id
FROM mypracticeschema.employees13 e
RIGHT JOIN mypracticeschema.departments13 d
    ON e.department_id = d.department_id;
    
    
    SELECT 
    e.emp_name AS employee,
    m.emp_name AS manager
FROM mypracticeschema.employees13 e
LEFT JOIN mypracticeschema.employees13 m
    ON e.manager_id = m.emp_id;
    
    
CREATE TABLE mypracticeschema.employees_india (
    emp_id INT,
    emp_name VARCHAR(50),
    department VARCHAR(50)
);


CREATE TABLE mypracticeschema.employees_usa (
    emp_id INT,
    emp_name VARCHAR(50),
    department VARCHAR(50)
);


INSERT INTO mypracticeschema.employees_india VALUES
(1, 'Rahul', 'IT'),
(2, 'Amit', 'HR'),
(3, 'Neha', 'Finance'),
(4, 'Priya', 'Testing');


INSERT INTO mypracticeschema.employees_usa VALUES
(3, 'Neha', 'Finance'),
(4, 'Priya', 'Testing'),
(5, 'John', 'IT'),
(6, 'David', 'HR');

select * from mypracticeschema.employees_india;

SELECT * FROM mypracticeschema.employees_india
UNION ALL
SELECT * FROM mypracticeschema.employees_usa;

SELECT i.*
FROM mypracticeschema.employees_india i
INNER JOIN mypracticeschema.employees_usa u
ON i.emp_id = u.emp_id;

SELECT i.*
FROM mypracticeschema.employees_usa i
LEFT JOIN mypracticeschema.employees_india u
ON i.emp_id = u.emp_id
WHERE u.emp_id IS NULL;


